import { Spin } from 'antd';
import React from 'react';

const Loading = () => {
    return (
        <div>
            <div>
      <Spin size="large" />
    </div>
        </div>
    );
};

export default Loading;